---
phase: 8
plan: 3
title: "Building Media & File Upload Expansion"
wave: 2
depends_on: [2]
files_modified:
  - src/config/propertyFields.js
requirements: [DATA-04, DATA-08, DATA-09]
autonomous: true
---

# Plan 08-03: Building Media & File Upload Expansion

## Objective
Expand file and photo capabilities to encompass Outside Spaces, Building Entries, and OC documents.

## Tasks
1. Refactor `ocFile` inside `propertyFields.js` from `type: toggle` to `type: file` so the user can actually upload the OC document.
2. In `propertyFields.js` (Property Info), add `entryToBuilding` as a new `type: file` block or as a toggle + `hasPhoto` element (depending on schema layout context, preferably use `type: file`).
3. Update `outsideSpace` in facilities to have `hasPhoto: true`. (This works natively with the existing `renderToggle` logic).

## Verification
- Upload zone works for OC Document.
- Upload zone works for Entry to Building.
- Toggling Outside Space reveals the photo upload icon safely.
