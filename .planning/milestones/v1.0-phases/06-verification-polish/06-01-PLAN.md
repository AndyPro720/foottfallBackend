# Plan 06-01: Admin Dashboard & Approval Flow

Implement the user management system to allow admins to approve sign-ups and manage roles.

## Tasks

- [ ] **Data Model & Service Layer**:
    - [ ] Update `src/backend/userRoleService.js` to set default `status: 'pending'` for new users.
    - [ ] Add `updateUserStatus(uid, status)` and `updateUserRole(uid, role)` functions.
    - [ ] Function to `getAllUsers()` for admin list.
- [ ] **Access Control (Rules)**:
    - [ ] Update `firestore.rules` to check for `get(/databases/$(database)/documents/users/$(request.auth.uid)).data.status == 'active'` for any write operation.
- [ ] **Router & Shell**:
    - [ ] Add `#admin` route in `src/main.js`.
    - [ ] Implement "Pending Approval" view in `main.js` which blocks dashboard access if status is not `active`.
    - [ ] Add "Admin Panel" link to the user profile dropdown (visible only to admins).
- [ ] **Admin UI**:
    - [ ] Create `src/pages/Admin.js`:
        - Fetch and display list of all users.
        - Buttons to Approve (`active`), Suspend, or Toggle role (`agent`/`admin`).
        - Loading and success/error states using `showToast`.

## Verification Plan

- [ ] Login as a New User -> Verify "Pending Approval" screen.
- [ ] Manual DB Update: Set one user to `admin` -> Verify they see the "Admin Panel" link.
- [ ] Use Admin Panel to activate the new user -> Verify they can now see the Dashboard.
- [ ] Verify that a `pending` user cannot write to inventory via console/bypass (Firestore Rules test).
