# Plan 06-02: UX/Offline Fixes & Visual Audit

Refine the offline experience and ensure the mobile-first UI is responsive and high-fidelity.

## Tasks

- [ ] **Offline Save Bug (UX)**:
    - [ ] Audit `src/pages/IntakeForm.js` submit handler.
    - [ ] Remove `await` on any `showToast` or `updateInventoryItem` that may hang while offline.
    - [ ] Ensure `window.location.hash = '#'` is the **very first thing** to happen after `createInventoryItem` if `!navigator.onLine`.
- [ ] **Visual Audit (CSS)**:
    - [ ] Audit `src/style.css` for consistent spacing (`var(--space-*)`).
    - [ ] Enhance mobile responsiveness for cards and tables.
    - [ ] Smooth transitions between pages.
    - [ ] Premium "SaaS" polish (glassmorphic elements optimization).
- [ ] **Connectivity Infrastructure**:
    - [ ] Ensure `ConnectivityBanner` is truly non-blocking and provides accurate status.
- [ ] **Verification**:
    - [ ] Manual walkthrough on desktop.
    - [ ] Manual walkthrough on mobile (simulated or real).
    - [ ] Final `npm run build` check.

## Verification Plan

- [ ] Verify instant redirect on offline save (No UI hang).
- [ ] Verify responsive table on Admin page.
- [ ] Verify consistent font-weights and colors across all views.
