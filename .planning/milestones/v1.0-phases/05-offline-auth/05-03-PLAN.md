# Plan 05-03: Offline Sync & Draft UI

Enable Firestore's offline persistence and add visual feedback for connectivity and sync status.

## Tasks

- [ ] Modify `src/backend/firebaseConfig.js`: 
    - Function to `enableMultiTabIndexedDbPersistence(db)`
    - Implement a `isOnline` watcher (using `window.addEventListener`)
- [ ] Create `src/components/ConnectivityBanner.js`:
    - Floating banner shown only when `navigator.onLine` is false
    - Styled with `bg-error` or `bg-warning` (glassmorphism)
- [ ] Modify `src/pages/Home.js` & `src/pages/PropertyDetail.js`:
    - Update property cards to listen to `snapshot.metadata.hasPendingWrites`
    - Prepend "Sync Pending" badge to the property title/badge area
- [ ] Modify `src/main.js`:
    - Inject `ConnectivityBanner` into the application shell

## Verification Plan

- [ ] Disable Network in Chrome DevTools -> Add a Property -> Verify "Sync Pending" badge appears
- [ ] Verify Property appears in Dashboard while offline
- [ ] Re-enable Network -> Verify "Sync Pending" badge disappears
- [ ] Verify Offline banner shows/hides correctly
