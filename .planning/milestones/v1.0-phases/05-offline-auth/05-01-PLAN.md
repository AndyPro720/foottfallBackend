# Plan 05-01: Authentication Foundation

Setup the core authentication system using Firebase Auth, including Google social login and a custom Login UI.

## Tasks

- [ ] Enable Google Auth in Firebase Console (User manual steps provided in instructions)
- [ ] Create `src/pages/Login.js` with:
    - Email/Password form fields
    - Google Sign-In button
    - Elegant glassmorphism styling
    - Loading states and toast notifications
- [ ] Modify `src/main.js`:
    - Add `auth.onAuthStateChanged` listener
    - Implement route guarding: redirect to `#login` if unauthorized for `#add`, `#edit`, or `#property/:id`
    - Inject "Profile" (initials/logout) into the Top Bar
- [ ] Update `src/backend/firebaseConfig.js`:
    - Export `GoogleAuthProvider` for use in `Login.js`

## Verification Plan

- [ ] Verify user can sign up with email
- [ ] Verify user can sign in with Google (manual check)
- [ ] Verify unauthorized access to `#add` redirects to `#login`
- [ ] Verify logout clears session and redirects
