---
phase: 03-pwa-scaffold-mobile-ui
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [src/pages/Home.js, src/pages/IntakeForm.js, src/main.js]
autonomous: true
requirements: [INTAKE-UI]
must_haves:
  truths:
    - User can navigate to the Add Inventory form
    - User can submit real data to the Firestore backend
    - User can view properties fetched from Firestore
  artifacts:
    - path: src/pages/IntakeForm.js
      provides: Form UI component
    - path: src/pages/Home.js
      provides: Dashboard list component
  key_links:
    - from: src/pages/IntakeForm.js
      to: src/backend/inventoryService.js
      via: import createInventoryItem
      pattern: 'inventoryService.*createInventoryItem'
    - from: src/pages/Home.js
      to: src/backend/inventoryService.js
      via: import getInventoryItems
      pattern: 'inventoryService.*getInventoryItems'
---

<objective>
Implement the primary interactive pages for the SPA: the intake form for registering new retail properties, and the home dashboard for displaying fetched inventory items. Connect these visually polished components to the Firebase services built in Phase 2.
Purpose: Complete the functional frontend so the app is useful for on-the-go data entry.
Output: Two Vanilla JS page components fully integrated into the `main.js` router.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pwa-scaffold-mobile-ui/03-UI-SPEC.md
</context>

<interfaces>
From src/backend/inventoryService.js:
```javascript
export const createInventoryItem = async (inventoryData) => { ... }
export const getInventoryItems = async () => { ... }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the Dashboard (Home.js)</name>
  <files>src/pages/Home.js, src/main.js</files>
  <action>
    Create `src/pages/Home.js` exporting a render function.
    The function should display a glassmorphic container for the dashboard.
    It must import `getInventoryItems` from `src/backend/inventoryService.js`, call it on load, and map the returned items into a list of HTML cards (`.glass-card`). Provide a generic loading spinner colored with the `--accent` white color from `03-UI-SPEC.md`.
    Update `src/main.js` router to load this component when hash is empty or `#`.
  </action>
  <verify>
    <automated>grep "import.*getInventoryItems" src/pages/Home.js</automated>
  </verify>
  <done>
    Home component exports logic that fetches and renders real inventory data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the Intake Form</name>
  <files>src/pages/IntakeForm.js, src/main.js</files>
  <action>
    Create `src/pages/IntakeForm.js` exporting a render function.
    Create an HTML form with inputs for basic property details (Name, Location, Price).
    The form MUST contain a primary submit button with the exact text: "Register New Retail Property" styled with white text that contrasts against the `--dominant` green theme.
    Attach a submit event listener that constructs an object and calls `createInventoryItem` from `src/backend/inventoryService.js`.
    Update `src/main.js` router to load this component when hash is `#add`.
  </action>
  <verify>
    <automated>grep "Register New Retail Property" src/pages/IntakeForm.js</automated>
  </verify>
  <done>
    Intake form properly wires up the submit event to the backend and honors the specific UI-SPEC copywriting.
  </done>
</task>

</tasks>

<verification>
No checkpoint required. Automated grep checks verify structural dependencies and copy.
</verification>

<success_criteria>
- Home.js built and wires up database read capabilities
- IntakeForm.js built with exact CTA text and form submission
- Both pages imported and routed in main.js
</success_criteria>

<output>
After completion, create `.planning/phases/03-pwa-scaffold-mobile-ui/03-02-SUMMARY.md`
</output>
