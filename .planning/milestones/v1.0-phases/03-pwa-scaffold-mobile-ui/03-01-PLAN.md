---
phase: 03-pwa-scaffold-mobile-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.js, index.html, src/main.js, src/style.css]
autonomous: true
requirements: [INTAKE-UI]
must_haves:
  truths:
    - User can load the web app locally
    - User sees basic UI elements with brand colors
    - App registers a service worker for PWA capabilities
  artifacts:
    - path: package.json
      provides: Vite dependencies
    - path: vite.config.js
      provides: PWA plugin configuration
    - path: src/main.js
      provides: Vanilla JS SPA initialization
  key_links:
    - from: index.html
      to: src/main.js
      via: script module import
      pattern: 'src=".*main.js"'
---

<objective>
Scaffold the foundational Vite PWA project and implement the core UI shell with the basic vanilla JS router. 
Purpose: Establish the frontend infrastructure necessary to build out the inventory intake tool without overwhelming executor context.
Output: Working Vite development server, PWA registration, global CSS design tokens, and a basic routing shell.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pwa-scaffold-mobile-ui/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite PWA Project</name>
  <files>package.json, vite.config.js, index.html</files>
  <action>
    Initialize a new Vite project in the root using vanilla JS (`npx -y create-vite@latest . --template vanilla` - you may need to overwrite or selectively add files if repo exists).
    Install `vite-plugin-pwa` (e.g., `npm install -D vite-plugin-pwa`).
    Create `vite.config.js` and configure the Vite PWA plugin with standard web manifest settings (name: "Footfall Inventory", short_name: "Inventory", theme_color: "#0a3622").
    Ensure `index.html` includes a viewport meta tag for mobile responsiveness.
  </action>
  <verify>
    <automated>npm install && npm run build</automated>
  </verify>
  <done>
    Build completes without errors and includes a basic PWA manifest and service worker output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement UI Shell & Design System Tokens</name>
  <files>src/style.css, src/main.js</files>
  <action>
    Clear default `src/style.css` and set up CSS variables matching `03-UI-SPEC.md`:
    - `--dominant: #0a3622`
    - `--secondary: rgba(255, 255, 255, 0.1)`
    - `--accent: #ffffff`
    - `--destructive: #ef4444`
    - Use the font `Inter` via Google Fonts URL.
    - Create a `.glass-card` utility class using backdrop-filter blur and `--secondary` background.
    
    Update `src/main.js` to initialize a simple vanilla JS hash-based router.
    Render a permanent bottom navigation bar (Home `#`, Add Inventory `#add`) with the glassmorphic styling.
  </action>
  <verify>
    <automated>grep "\--dominant.*#0a3622" src/style.css</automated>
  </verify>
  <done>
    CSS tokens precisely match the brand specs, and `main.js` houses the routing and shell HTML logic.
  </done>
</task>

</tasks>

<verification>
No checkpoint required. Automated build and grep verification suffices for basic scaffolding.
</verification>

<success_criteria>
- Vite build succeeds
- CSS file contains brand variables
- PWA manifest correctly generated during build
</success_criteria>

<output>
After completion, create `.planning/phases/03-pwa-scaffold-mobile-ui/03-01-SUMMARY.md`
</output>
