---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Firebase Backend Initialization & Schema Design

## Objective
Initialize the Firebase backend structure and design the Firestore schema for inventory intake, paving the way for future CRM features.

## Context
- .gsd/SPEC.md
- .gsd/PROJECT.md
- .gsd/phases/1/RESEARCH.md

## Tasks

<task type="auto">
  <name>Scaffold Firebase Config</name>
  <files>
    - src/backend/firebaseConfig.js
  </files>
  <action>
    - Scaffold a `firebaseConfig.js` file that initializes the Firebase app using environment variables.
    - Export `db` (Firestore), `auth` (FirebaseAuth), and `storage` (FirebaseStorage).
    - Do not hardcode any sensitive keys; rely on `.env` loaded via Vite.
  </action>
  <verify>cat src/backend/firebaseConfig.js</verify>
  <done>firebaseConfig.js exists and exports initialized services securely using import.meta.env</done>
</task>

<task type="auto">
  <name>Define Firestore Schema</name>
  <files>
    - .gsd/phases/1/SCHEMA.md
  </files>
  <action>
    - Document the proposed Firestore NoSQL schema for inventory (SKU, name, dimensions, location, images).
    - Structure it to support a future CRM (e.g., separating `/inventory/{itemId}` from `/users/{userId}`).
    - Define expected NoSQL indexes, query patterns, and basic Firestore Security Rules.
  </action>
  <verify>cat .gsd/phases/1/SCHEMA.md</verify>
  <done>SCHEMA.md accurately defines collections, fields, and security policies.</done>
</task>

## Success Criteria
- [ ] Firebase configuration code is scaffolded.
- [ ] Firestore schema is fully documented in `SCHEMA.md` and aligns with CRM extensibility.
